/* Radian source file specification */
(
{
  Identifier = sourcecode.radian;
  BasedOn = sourcecode;
  Name = "Radian Files";
  Extensions = (radian);
  MIMETypes = ("text/radian");
  ComputerLanguage = radian;
  IsTextFile = YES;
}

)